export enum UserStatus {
  NEW = 'new',
  PENDING = 'pending',
  ACTIVE = 'active',
}

export enum PaymentMethod {
  PREPAID = 'prepaid',
  POSTPAID = 'postpaid',
}

export enum OrderStatus {
  NEW = 'new',
  PENDING = 'pending',
  COMPLETED = 'completed',
  CANCELED = 'canceled',
}